trigger:
- main

pool:
  name: AGENTPOOL
  vmImage: ubuntu-latest

steps:
- script: echo "single line job"
  displayName: 'hello'

- script: |
    echo multiline start
    echo multiline end
  displayName: multiline job 
