trigger:
- main

pool:
  name: AGENTPOOL
  vmImage: ubuntu-latest

steps:
- script: echo "single line job"
  displayName: 'hello'

- script: |
    echo multiline start
    echo multiline end
    python --version
  displayName: multiline job 
